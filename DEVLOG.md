앱을 실행하면 먼저 **알림 권한과 위치 권한을 요청**하고, 사용자가 권한이 제대로 동작하는지 직접 확인할 수 있도록 **테스트 알림 버튼**과 **현재 위치를 표시하는 지도**를 홈 화면에 추가하는 것

---

# 1. 프로젝트를 생성하고 기본 권한 요청 기능을 구현했다.

`create-expo-app`로 Expo(TypeScript) 프로젝트를 생성했다. 

이후 `expo-notifications`와 `expo-location`을 설치하고 `app.json`에 위치 권한과 알림 권한 관련 설정을 추가했다.

`App.tsx`에서는 앱이 처음 실행될 때(`useEffect`) 알림 권한을 먼저 요청하고, 이후 위치 권한을 요청하도록 구현했다. 또한 현재 권한 상태를 화면에 "허용됨", "거부됨"으로 표시해 사용자가 바로 확인할 수 있도록 했다.

개발 속도를 높이기 위해 실제 휴대폰 대신 `expo start --web`을 실행하여 **브라우저에서 먼저 화면을 확인**했고, 기본 화면과 권한 요청 로직은 정상적으로 동작했다.

---

# 2. 테스트 알림 버튼과 지도를 추가하면서 첫 번째 문제가 발생했다.

다음 단계는 권한이 실제로 잘 동작하는지 확인할 수 있는 기능을 추가하는 것이었다.

사용자가 버튼을 누르면 테스트 알림이 오도록 만들고, 현재 위치를 지도에 표시하도록 구현했다.

처음에는 가장 많이 사용하는 `react-native-maps`를 이용해 Google Maps를 표시하도록 만들었다.

그런데 웹 프리뷰를 실행하자 앱이 바로 크래시가 발생했다.

---

## 왜 갑자기 웹에서 문제가 발생했을까?

앱을 만들고 있었는데 왜 웹 이야기가 나오는지 의아할 수 있다.

Expo는 하나의 프로젝트를 Android뿐 아니라 웹에서도 실행할 수 있다.

실제 Android 앱을 빌드해서 설치하는 데는 시간이 오래 걸리기 때문에, 개발 초기에는 대부분 웹 프리뷰를 이용해 UI가 제대로 보이는지 먼저 확인한다.

나 역시 같은 방식으로 개발하고 있었다.

그런데 `react-native-maps`는 Android와 iOS 전용 라이브러리이다.

웹에서는 사용할 수 있는 구현 자체가 존재하지 않는다.

문제는 파일 최상단에서

```tsx
import MapView from 'react-native-maps';
```

처럼 정적으로 import하고 있었다는 점이었다.

JavaScript는 화면을 그리기 전에 먼저 import된 라이브러리를 모두 읽는다.

즉, 실제로 지도를 사용하기도 전에 웹이 `react-native-maps`를 읽으려고 시도했고, 웹 구현이 없었기 때문에 앱 전체가 실행되지 못한 것이다.

즉, **지도 컴포넌트가 문제가 아니라 import 자체가 문제였다.**

---

## 어떻게 해결했을까?

웹에서는 아예 `react-native-maps`를 읽지 않도록 변경했다.

`Platform.OS !== 'web'`인 경우에만 `require('react-native-maps')`로 동적으로 불러오도록 수정했다.

이렇게 하자 Android에서는 기존처럼 지도를 사용할 수 있었고, 웹에서는 지도 대신 안내 문구만 출력하면서 앱이 정상적으로 실행되었다.

---

# 3. Android에서 테스트하다가 Expo Go의 한계를 발견했다.

웹에서는 문제가 없어 보였기 때문에 이번에는 실제 Android 휴대폰에서 Expo Go를 이용해 테스트를 진행했다.

그런데 앱을 실행하자마자 바로 종료되었다.

에러 메시지를 확인해 보니 `expo-notifications`가 Android의 Expo Go에서는 더 이상 지원되지 않는다는 내용이었다.

---

## 왜 Expo Go에서는 알림 기능을 막아 놓았을까?

처음에는 "알림만 안 되게 하면 되는데 왜 import만 해도 앱이 죽지?"라는 의문이 들었다.

조사해 보니 이유는 Expo Go의 구조 때문이었다.

Expo Go는 내가 만든 앱이 아니다.

Expo에서 미리 만들어 배포한 하나의 공용 앱 안에서 내 JavaScript 코드만 실행되는 구조이다.

반면 알림 기능은 단순한 JavaScript 기능이 아니다.

Android의 Notification Manager와 연결되고,

- AndroidManifest 설정
- Notification Channel 생성
- Firebase 설정
- 백그라운드 서비스

등 여러 네이티브(Android) 설정이 함께 필요하다.

이런 설정은 앱마다 모두 다르다.

Expo Go 하나가 모든 개발자의 앱 설정을 미리 포함하는 것은 사실상 불가능하다.

그래서 Expo는 SDK 53부터 Android의 Expo Go에서는 알림 기능을 아예 제거하고, 대신 **Dev Build**에서만 지원하도록 구조를 변경했다.

---

## 그런데 왜 import만 해도 예외를 발생시켰을까?

보통 라이브러리는 함수를 호출했을 때 에러를 발생시키는 경우가 많다.

그런데 `expo-notifications`는 import하는 순간부터 예외를 발생시켰다.

라이브러리 내부를 확인해 보니 모듈이 로드되는 순간

- Android인지 확인하고
- Expo Go인지 확인한 뒤
- 조건이 맞으면 즉시 `throw`하도록 구현되어 있었다.

이렇게 만든 이유는 **지원하지 않는 환경에서 개발자가 계속 디버깅하는 것을 막기 위해서**이다.

만약 import는 성공하고 실제 알림을 보낼 때만 실패했다면 개발자는 권한 문제인지, 코드 문제인지, 비동기 문제인지 계속 엉뚱한 곳을 의심하게 된다.

반대로 import 단계에서 바로 "이 환경에서는 사용할 수 없습니다."라고 알려주면 문제의 원인이 코드가 아니라 **실행 환경**이라는 것을 즉시 알 수 있다.

즉, 일부러 불편하게 만든 것이 아니라 **잘못된 방향으로 디버깅하지 않도록 조기에 실패(Early Fail)** 하도록 설계한 것이다.

---

# 4. 실제 알림을 테스트하기 위해 Dev Build를 만들기로 했다.

Expo Go에서는 더 이상 테스트할 수 없다는 것을 알게 되면서 Dev Build를 만들기로 했다.

하지만 여기서부터는 기능 구현보다 개발 환경 문제가 훨씬 많이 발생했다.

---

## USB가 연결되지 않았다.

처음에는 USB를 연결했는데도 `adb devices`에 아무 기기도 나타나지 않았다.

USB 디버깅 설정이나 개발자 옵션을 계속 확인했지만 원인을 찾지 못했다.

나중에 알고 보니 사용하던 케이블이 **충전 전용 케이블**이었다.

전원은 공급되지만 데이터 통신 자체가 불가능했던 것이다.

---

## 무선 디버깅도 계속 실패했다.

USB 대신 Wireless ADB도 시도했다.

하지만 `adb pair`를 실행할 때마다

```
protocol fault
```

오류가 계속 발생했다.

처음에는 방화벽이나 공유기 문제를 의심했다.

하지만 나중에 알고 보니 원인은 훨씬 단순했다.

Android의 무선 디버깅 페어링 코드는 약 1분 정도만 유효하다.

사용자가 화면을 보고 IP와 포트를 알려주는 사이 이미 코드가 만료되어 오래된 포트로 계속 연결을 시도하고 있었던 것이다.

최신 포트 번호로 다시 시도하자 바로 연결되었다.

---

## 이번에는 APK 설치가 실패했다.

무선 연결에는 성공했지만 이번에는 APK가 설치되지 않았다.

원인은 에뮬레이터용 APK를 실제 휴대폰에 설치하려고 했기 때문이었다.

Android 에뮬레이터는 보통 x86_64 CPU를 사용하고, 실제 휴대폰은 ARM64 CPU를 사용한다.

CPU 구조가 다르기 때문에 에뮬레이터용 APK를 휴대폰에 설치할 수 없었던 것이다.

실제 휴대폰만 연결한 상태에서 다시 빌드하자 정상적으로 설치되었다.

---

# 5. 설치는 되었지만 앱이 실행되자마자 종료되었다.

앱은 설치되었지만 실행하자마자 `NoClassDefFoundError`가 발생하며 종료되었다.

로그를 확인해 보니 존재하지 않는 네이티브 클래스를 계속 찾고 있었다.

---

## 왜 이런 문제가 발생했을까?

프로젝트를 진행하는 동안

- `react-native-maps`
- `react-native-webview`

등 여러 라이브러리를 설치하고 제거했다.

하지만 Android 네이티브 프로젝트는 예전 상태를 그대로 가지고 있었다.

즉,

현재 `node_modules`와

Android 프로젝트가 서로 다른 상태가 된 것이다.

---

## 어떻게 해결했을까?

`android` 폴더를 삭제한 뒤

```
npx expo prebuild --clean
```

을 실행하여 Android 프로젝트를 처음부터 다시 생성했다.

그러자 현재 라이브러리 목록에 맞는 네이티브 프로젝트가 다시 생성되었고 앱도 정상적으로 실행되었다.

---

# 6. 이번에는 알림 버튼이 아무 반응도 하지 않았다.

앱은 실행되는데 테스트 알림 버튼을 눌러도

- 성공 메시지도 없고
- 실패 메시지도 없고
- 아무 일도 일어나지 않았다.

처음에는 권한 문제를 의심했고, 이후에는 Promise 문제도 의심했다.

하지만 여러 로그를 확인해 보니 `import()`가 끝나지 않고 계속 대기하고 있었다.

Metro 번들러와 연결되는 과정에서 동적 import가 정상적으로 완료되지 못하고 있었던 것이다.

그래서 `await import(...)` 대신 `require(...)`를 사용하도록 변경했다.

`require()`는 이미 번들에 포함된 모듈을 즉시 가져오기 때문에 더 이상 대기하지 않았고, 이후 알림도 정상적으로 동작했다.

---

# 7. Google Maps 대신 Kakao Maps로 변경했다.

앱에서 Kakao Maps를 사용하기 때문에 변경하였다.

React Native에는 공식 Kakao Maps 라이브러리가 없기 때문에 `react-native-webview` 안에서 Kakao Maps JavaScript SDK를 실행하는 방식을 선택했다.

이 방식은 웹 페이지를 앱 안에 띄운 뒤 그 안에서 Kakao 지도를 실행하는 구조이다.

---

# 8. Kakao 지도가 계속 보이지 않았다.

처음에는 단순한 설정 문제라고 생각했다.

하지만 실제로는 여러 문제가 동시에 존재하고 있었다.

---

## 첫 번째 문제는 Web 플랫폼 등록이었다.

Kakao Maps JavaScript SDK는 등록된 도메인에서만 사용할 수 있다.

그런데 Kakao Developers 콘솔에 Web 플랫폼이 등록되어 있지 않아 SDK가 정상적으로 동작하지 않았다.

Web 플랫폼을 등록한 뒤 다시 테스트했다.

---

## 두 번째 문제는 등록한 주소가 달랐다.

이번에는 플랫폼은 등록했지만 실제 WebView에서 사용하는 주소와 Kakao 콘솔에 등록한 주소가 서로 달랐다.

Kakao는 주소가 정확히 일치해야만 SDK를 허용한다.

등록된 주소와 실제 주소를 동일하게 맞추자 이 문제는 해결되었다.

---

## 세 번째 문제는 지도 서비스를 활성화하지 않았다.

키는 발급받았지만 Kakao 콘솔에서 지도(Map) 제품 자체를 활성화하지 않은 상태였다.

앱 키를 만들었다고 해서 모든 기능이 자동으로 활성화되는 것은 아니었다.

지도 서비스를 ON으로 변경하자 다음 단계로 진행할 수 있었다.

---

## 네 번째 문제는 WebView 설정이었다.

그래도 여전히 빈 회색 화면만 나타났다.

https://bori-note.tistory.com/70

참고 블로그와 코드를 하나씩 비교해 보니

- JavaScript 활성화
- DOM Storage 활성화
- 지도 생성 시점
- `baseUrl`
- WebView 내부 로그 확인 방식

등이 모두 조금씩 달랐다.

이 설정들을 하나씩 수정한 뒤에야 지도가 정상적으로 표시되었다.

특히 WebView 내부의 `console.log`를 React Native 화면으로 전달하도록 수정하면서 이후에는 `adb logcat`을 보지 않아도 화면만 보고 오류를 확인할 수 있게 되었다.

---

# 프로젝트를 진행하며 계속 반복된 환경 문제

기능과 직접 관련은 없지만 개발을 방해했던 환경 문제도 계속 발생했다.

무선 디버깅은 휴대폰을 재부팅할 때마다 다시 연결해야 했고, 권한 팝업은 반복 테스트를 하다 보니 Android가 더 이상 표시하지 않는 상태가 되기도 했다. 또한 휴대폰 화면이 잠겨 있어 원격 로그 확인이 실패하는 경우도 많았고, Gradle 빌드와 `npm install`을 동시에 실행하면서 파일 잠금 오류가 발생하기도 했다.

---

# 마무리

처음에는 단순히 **"알림 버튼과 현재 위치 지도를 추가하는 앱"**을 만드는 프로젝트라고 생각했다.

하지만 실제로는 기능 구현보다 **Expo Go의 제약을 이해하고, Dev Build 환경을 구축하고, Android 네이티브 빌드 문제를 해결하고, Kakao Maps SDK가 동작하기 위한 조건을 하나씩 찾아가는 과정**이 프로젝트의 대부분을 차지했다.

특히 이번 프로젝트를 통해 단순히 "코드를 작성하는 것"보다 **실행 환경을 이해하고, 문제의 원인을 추론하며 하나씩 제거하는 디버깅 과정이 실제 개발에서 훨씬 더 중요한 비중을 차지한다**는 점을 경험할 수 있었다.